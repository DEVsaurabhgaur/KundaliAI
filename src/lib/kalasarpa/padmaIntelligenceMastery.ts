export function evaluatePadmaImpact(rahuHouse: number): string {
  return rahuHouse === 5 ? 'Padma Kala Sarpa: High intellectual creativity, academic honors, and expansive social network.' : 'Non-Padma Alignment.';
}
